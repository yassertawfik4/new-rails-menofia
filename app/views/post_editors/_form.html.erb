<%= form_with(model: post_editor) do |form| %>
  <% if post_editor.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post_editor.errors.count, "error") %> prohibited this post_editor from being saved:</h2>

      <ul>
        <% post_editor.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :post_id, style: "display: block" %>
    <%= form.text_field :post_id %>
  </div>

  <div>
    <%= form.label :editor_id, style: "display: block" %>
    <%= form.text_field :editor_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
